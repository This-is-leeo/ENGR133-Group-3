ok, so the auto grader still doesnt work (9/25/2024 5:51 pm) so the code that gets somewhat full score on the autograder is Py3_Task6.py, 
I optimized the Ind1 so it is shorter (and also unreadable... ) so it can pass the code too long restriction
